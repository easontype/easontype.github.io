# easontype.github.io — 專案規則

## 文章內容排版

部落格文章內容一律**禁止用方框、卡片（card/box）排版**，改用純文字敘述＋列點（bullet/numbered list）呈現：

- 粗體小標題
- 條列重點（3 個左右即可，不要太多——例如價格/免費狀態、學習時間、相關學習資訊）
- 再加一兩句話文字描述

視覺樣式沿用 `src/pages/blog/[slug].astro` 既有的 `.prose`（標題＋列點＋段落＋左框線 blockquote），不要新增 `.card`/`.box`/grid 卡片式 CSS 元件。
